{{> preloader}}
{{> loggedNavbar}}

<body>
    <main>
        <div class="container" style="margin-top: 5%;">
            <h5 style="font-family: Poppins; font-size: 32px">My offers</h5>
            <div class="row align-items-center" style="margin-bottom: 15%;">
                {{#each offers as |offer|}}
                    {{> homes}}
                    {{/each}}
            </div>
        </div>
    </main>
</body>